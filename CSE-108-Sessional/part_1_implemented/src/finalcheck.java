import java.io.BufferedWriter;
import java.io.*;
import java.io.FileWriter;

public class finalcheck {
    public void checker(){

        System.out.println("If new movie has been added to the list it has been added to files," +
                "And if it isn,t added its an exit command ");
       // System.out.println(MovieList.movieList.get(MovieList.movieList.size()-1).Revenue);


        //out.close();
    }
}
